export interface IAdvertisement {
    categoryId: string;
    name: string;
    description: string;
    image: string;
}

export interface IAdvertisementResponse {
    id: number
    categoryId: string;
    name: string;
    description: string;
    image: string;
}